#ifndef STREETRACER_CAMERAMANAGER_H
#define STREETRACER_CAMERAMANAGER_H

class CameraManager
{
 public:
  CameraManager* GetSingletonPtr();
  CameraManager& Get
 private:
  CameraManager();
  ~CameraManager();
};

#endif
